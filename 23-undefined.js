
// undefine pada variable
let nama;
// nama = "a"

if (nama === undefined)
    console.warn('UNDIFINED')
else 
    console.info('DEFINED')

// undefine pada array
const nama2 = ['fajar', 'fadilah', 'harun']

if (nama2[3] === undefined)
    console.warn('UNDIFINED')
else 
    console.info('DEFINED')

// undefine pada object property
const nama3 = {}

if (nama2.nama === undefined)
    console.warn('UNDIFINED')
else 
    console.info('DEFINED')
